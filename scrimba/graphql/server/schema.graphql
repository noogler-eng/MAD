# node 1
type User {
  id: ID!
  name: String!
  posts: [Post!]!
}

# node 2
type Post {
  id: ID!
  title: String!
}

# ! can't be null
# user query, returns User, id mandatory
type Query {
  user(id: ID!): User
}
